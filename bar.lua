local colors = require("colors")

-- Equivalent to the --bar domain
sbar.bar({
  height = 30,
  color = colors.bar.transparent,
  padding_right = 0,
  padding_left = 0,
  y_offset = -5,
  position = "top",
})
